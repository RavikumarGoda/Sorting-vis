import type { SortingAlgorithmType } from './types';

export const MIN_ARRAY_SIZE = 5;
export const MAX_ARRAY_SIZE = 100;
export const DEFAULT_ARRAY_SIZE = 50;

export const DEFAULT_SPEED = 250; // Medium speed

export const DEFAULT_ALGORITHM: SortingAlgorithmType = 'bubble';
